<form method="post" action="{{ path('app_commentaire_delete', {'idcomment': commentaire.idcomment}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commentaire.idcomment) }}">
    <button class="btn">Delete</button>
</form>
